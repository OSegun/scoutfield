"""
ScoutPlan: the deployable artefact. Roadmap item 9.

Roadmap items 1-8 answer a research question. This turns the answer into
something a person can run: export the classifier to ONNX, measure real inference
latency on real hardware, and put a usable interface in front of it.

Measure latency, do not estimate it. An estimate is not a result, and "runs at
interactive speed on commodity hardware" is a claim that has to be earned with a
number and a stated machine.
"""
